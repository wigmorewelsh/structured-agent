struct Address {
    city: String,
}

struct Person {
    name: String,
    address: Address,
}

fn main(): String {
    let addr = Address { city: "London" }
    let p = Person { name: "Alice", address: addr }
    return p.name
}
