[package]
name = "structured-agent-ast"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
structured-agent-runtime = { path = "../structured-agent-runtime" }
nonempty = "0.10"
